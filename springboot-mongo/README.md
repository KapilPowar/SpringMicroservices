Dockerised SpringBoot application with MongoDB

Just run docker-compose up -d ..

REST APIs -- 

GET localhost:8080/books 

POST localhost:8080/books 
             
             
                  JSON {
                        "id" : 102,
                        "name" : "Spring",
                        "authorName" : "Kapil"
                       }
